{
  "name": "deploy-yes-another-todolist",
  "author": {
    "name": "Marcos",
    "website": "https://www.github.com/mapten"
  },
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "if-env TYPE=server && (cd server && pnpm install) || (cd client && pnpm install)",
    "server": "cd server && pnpm start",
    "client": "cd client && pnpm start",
    "runall": "npm-run-all --parallel server client"
  },
  "devDependencies": {
    "if-env": "^1.0.4",
    "npm-run-all": "^4.1.5"
  }
}
